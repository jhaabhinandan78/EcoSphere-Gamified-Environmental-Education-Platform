<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/emptyStateContainer"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp"
    android:visibility="gone">

    <ImageView
        android:id="@+id/ivEmptyStateIcon"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_marginBottom="24dp"
        android:src="@android:drawable/ic_menu_search"
        android:alpha="0.6"
        android:contentDescription="@string/content_description_empty_state" />

    <TextView
        android:id="@+id/tvEmptyStateTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/error_no_data"
        android:textAppearance="@style/TextAppearance.EcoLearn.Headline3"
        android:textAlignment="center"
        android:layout_marginBottom="8dp" />

    <TextView
        android:id="@+id/tvEmptyStateSubtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Please try again later"
        android:textAppearance="@style/TextAppearance.EcoLearn.Body2"
        android:textAlignment="center"
        android:layout_marginBottom="24dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnEmptyStateAction"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/retry"
        style="@style/Widget.EcoLearn.Button.Outlined"
        android:visibility="gone" />

</LinearLayout>